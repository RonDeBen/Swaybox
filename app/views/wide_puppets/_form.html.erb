<%= form_for(@wide_puppet) do |f| %>
  <% if @wide_puppet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wide_puppet.errors.count, "error") %> prohibited this wide_puppet from being saved:</h2>

      <ul>
      <% @wide_puppet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
