<p id="notice"><%= notice %></p>

<h1>Listing Wide Puppets</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @wide_puppets.each do |wide_puppet| %>
      <tr>
        <td><%= link_to 'Show', wide_puppet %></td>
        <td><%= link_to 'Edit', edit_wide_puppet_path(wide_puppet) %></td>
        <td><%= link_to 'Destroy', wide_puppet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Wide puppet', new_wide_puppet_path %>
