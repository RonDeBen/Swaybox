<%= form_for(@reg_status) do |f| %>
  <% if @reg_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reg_status.errors.count, "error") %> prohibited this reg_status from being saved:</h2>

      <ul>
      <% @reg_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
