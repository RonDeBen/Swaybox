<%= form_for(@shot_breakdown) do |f| %>
  <% if @shot_breakdown.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shot_breakdown.errors.count, "error") %> prohibited this shot_breakdown from being saved:</h2>

      <ul>
      <% @shot_breakdown.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
