%h1 Wardrobes
=link_to 'New Wardrobe', new_wardrobe_path, class: "btn  btn-info btn-block"
%br/
%table.table.table-striped.table-bordered.table-hover
  %thead
    %tr
      %th Name
      %th Status
      %th{:colspan => "2"} Commands
  %tbody
    - @wardrobes.order('wardrobes.name asc').each do |drobe|
      %tr
        %td= drobe.name
        %td= RegStatus.find_by(id: drobe.status).stage
        %td= link_to 'Edit', edit_wardrobe_path(drobe), class: "btn btn-warning btn-block"
        %td= link_to 'Destroy', drobe, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-block"
%br/
