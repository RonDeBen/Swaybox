<%= form_for(@mc_status) do |f| %>
  <% if @mc_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mc_status.errors.count, "error") %> prohibited this mc_status from being saved:</h2>

      <ul>
      <% @mc_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
